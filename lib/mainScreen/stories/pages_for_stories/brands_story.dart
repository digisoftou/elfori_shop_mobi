import 'package:flutter/material.dart';

import '../../../colors.dart';

class BrandsStory extends StatelessWidget {
  const BrandsStory({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: AppColors.grey,
    );
  }
}
