import 'package:flutter/material.dart';

import '../../../colors.dart';

class MansStory extends StatelessWidget {
  const MansStory({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: AppColors.grey,
    );
  }
}
